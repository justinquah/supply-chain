---
phase: 01-foundation-roles
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - supabase/migrations/0011_role_cleanup.sql
  - supabase/migrations/0012_drop_integration_tokens.sql
autonomous: false
requirements: [AUTH-01]

must_haves:
  truths:
    - "The user_role enum contains the value ACCOUNTS"
    - "No row in public.profiles has a role of SUPER_ADMIN, GENERAL, LOGISTICS, WAREHOUSE, or SUPPLIER after the migration runs"
    - "profiles.role has no column default (new users must be assigned a role at invite time)"
    - "is_super_admin() returns the same result as has_role('ADMIN')"
    - "Every RLS policy that previously referenced is_super_admin() or SUPER_ADMIN now grants the same access via has_role('ADMIN')"
    - "The integration_tokens and sync_log tables no longer exist"
    - "supabase db push applies cleanly against the linked project"
  artifacts:
    - path: "supabase/migrations/0011_role_cleanup.sql"
      provides: "ACCOUNTS enum add, deprecated-role row remap, default drop, trigger update, RLS policy/function rewrite"
      contains: "ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'ACCOUNTS'"
    - path: "supabase/migrations/0012_drop_integration_tokens.sql"
      provides: "Drops the Shopee integration_tokens and sync_log tables"
      contains: "DROP TABLE IF EXISTS"
  key_links:
    - from: "supabase/migrations/0011_role_cleanup.sql"
      to: "public.profiles rows"
      via: "UPDATE profiles SET role='ADMIN' WHERE role IN (deprecated roles)"
      pattern: "UPDATE public.profiles"
    - from: "supabase/migrations/0011_role_cleanup.sql"
      to: "RLS policies"
      via: "has_role('ADMIN') replaces is_super_admin() across all affected policies"
      pattern: "has_role\\('ADMIN'\\)"

user_setup:
  - service: supabase
    why: "Applying migrations to the hosted Postgres requires a linked project and access token"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (for non-interactive `supabase db push`)"
    dashboard_config:
      - task: "Ensure the local CLI is linked to the production project (supabase link) OR paste 0011 then 0012 into the Supabase SQL Editor in order"
        location: "Supabase Dashboard -> SQL Editor (fallback if CLI push is unavailable)"
---

<objective>
Migrate the Postgres role model to the four canonical roles. Add `ACCOUNTS` to the `user_role` enum, remap every deprecated-role profile row to `ADMIN`, drop the `'GENERAL'` column default, update the `handle_new_user` trigger to require an explicit role, fold `SUPER_ADMIN` into `ADMIN` across all RLS policies and the `is_super_admin()` helper, add `ACCOUNTS` where Phase-4 PO access needs it, and drop the dead Shopee `integration_tokens` / `sync_log` tables. Then apply the migrations to the live database.

Purpose: Satisfies AUTH-01 at the data layer (a user is one of exactly four roles; deprecated roles carry no rows) and implements D-01/D-02/D-03/D-07/D-11. The enum and RLS are the source of truth the app layer (01-03) builds on.
Output: Two append-only forward migrations (0011, 0012) plus a verified `supabase db push`.
</objective>

<execution_context>
@/Users/boonsunquah/Supply chain project/.claude/gsd-core/workflows/execute-plan.md
@/Users/boonsunquah/Supply chain project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-roles/01-CONTEXT.md
@.planning/phases/01-foundation-roles/01-RESEARCH.md
@supabase/migrations/0001_initial_schema.sql
@supabase/migrations/0010_integration_tokens.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0011_role_cleanup.sql (enum add, row remap, default drop, trigger, RLS/function rewrite)</name>
  <read_first>
    - supabase/migrations/0001_initial_schema.sql lines 18-145 (user_role enum definition line 22; profiles.role default 'GENERAL' line 95; handle_new_user trigger lines 109-118; current_user_role/has_role/is_super_admin lines 128-140)
    - supabase/migrations/0001_initial_schema.sql lines 405-640 (every RLS policy referencing roles — see the blast-radius table in 01-RESEARCH.md)
    - .planning/phases/01-foundation-roles/01-RESEARCH.md "Pattern 3: Postgres Enum Migration" and "Blast Radius: Full user_role Reference Map"
  </read_first>
  <action>
    Create a NEW append-only forward migration `supabase/migrations/0011_role_cleanup.sql` (do not edit any existing migration). It must perform the following, in order, with NO surrounding BEGIN/COMMIT transaction block (the bare `ALTER TYPE ... ADD VALUE` cannot run inside a transaction — research Pitfall 1):

    1. Enum add: a single bare statement `ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'ACCOUNTS';` per D-01. Do not attempt to DROP any enum value (Postgres cannot).

    2. Row remap (D-02): `UPDATE public.profiles SET role = 'ADMIN' WHERE role IN ('SUPER_ADMIN','GENERAL','LOGISTICS','WAREHOUSE','SUPPLIER');` so no row carries a deprecated role.

    3. Default drop (D-03): `ALTER TABLE public.profiles ALTER COLUMN role DROP DEFAULT;` so invites must supply a role.

    4. Trigger update (D-03): `CREATE OR REPLACE FUNCTION public.handle_new_user()` that inserts into profiles reading role directly from `(NEW.raw_user_meta_data->>'role')::user_role` with NO `'GENERAL'` fallback (keep the name COALESCE on the name field; remove the role COALESCE). Preserve `SECURITY DEFINER` and the existing insert columns (id, email, name, role).

    5. Function rewrite (D-07): `CREATE OR REPLACE FUNCTION is_super_admin() RETURNS BOOLEAN ... SELECT has_role('ADMIN');` so existing policies that still call it grant ADMIN access. Keep `current_user_role()` and `has_role(...)` unchanged.

    6. RLS policy rewrite — for EVERY policy in 0001 that references a deprecated role or `is_super_admin()`, DROP and recreate it referencing only the four canonical roles (and adding `ACCOUNTS` where noted). Cover all of these (names and tables from the blast-radius table): `profiles_select` (drop SUPER_ADMIN → `has_role('SCM','ADMIN')`), `profiles_update_self` (keep `id = auth.uid()`; the `OR is_super_admin()` now resolves to ADMIN via the rewritten function — recreate it explicitly as `id = auth.uid() OR has_role('ADMIN')`), `profiles_admin_all` (`has_role('ADMIN')` both USING and WITH CHECK), `pc_write`, `products_read` (drop GENERAL/LOGISTICS/WAREHOUSE/SUPPLIER branch; grant read to `has_role('SCM','ACCOUNTS','FINANCE','ADMIN')`), `products_write`, `skum_write`, `sales_read`, `sales_write`, `stock_read`, `stock_write`, `po_read` (replace with `has_role('SCM','ACCOUNTS','FINANCE','ADMIN')` — add ACCOUNTS per research open-question #3), `po_write` (`has_role('SCM','ACCOUNTS','ADMIN')`), `poli_read`, `poli_write`, `ship_read`, `ship_write`, `pod_read`, `pod_write` (`has_role('SCM','ACCOUNTS','ADMIN')` — ACCOUNTS uploads PO docs in Phase 4), `pay_read`, `pay_write`, `sr_read`, `sr_write`, `rp_read`, `rp_write`, `audit_read` (`has_role('ADMIN')`), `notif_read`/`notif_update` (`user_id = auth.uid() OR has_role('ADMIN')`), and `storage_delete` (`... AND has_role('ADMIN')`). Each must be `DROP POLICY IF EXISTS <name> ON <table>;` then `CREATE POLICY <name> ...` preserving the original command (SELECT/ALL/INSERT/UPDATE/DELETE), the `TO authenticated` clause, and any non-role predicate (e.g. `id = auth.uid()`, `supplier_id = auth.uid()` joins) EXCEPT drop SUPPLIER-only branches (no SUPPLIER rows remain). Do NOT touch the `po_status` enum (Phase 4) or any non-role policy logic.

    Add a top-of-file SQL comment block stating this is migration 0011, that it is append-only, and that ALTER TYPE ADD VALUE must stay outside a transaction. Do NOT reintroduce any deprecated role literal in a policy grant.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/0011_role_cleanup.sql` exists
    - `grep -c "ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'ACCOUNTS'" supabase/migrations/0011_role_cleanup.sql` is 1
    - `grep -c "UPDATE public.profiles" supabase/migrations/0011_role_cleanup.sql` is at least 1 and the statement's WHERE clause lists SUPER_ADMIN, GENERAL, LOGISTICS, WAREHOUSE, SUPPLIER
    - `grep -c "ALTER COLUMN role DROP DEFAULT" supabase/migrations/0011_role_cleanup.sql` is 1
    - The file contains no `BEGIN;` or `COMMIT;` wrapping the ALTER TYPE statement
    - In policy CREATE statements (grant predicates), the literals 'SUPER_ADMIN', 'GENERAL', 'LOGISTICS', 'WAREHOUSE' do not appear: `grep -nE "has_role\([^)]*('SUPER_ADMIN'|'GENERAL'|'LOGISTICS'|'WAREHOUSE')" supabase/migrations/0011_role_cleanup.sql` returns no matches
    - `is_super_admin` body delegates to `has_role('ADMIN')`: `grep -A2 "FUNCTION is_super_admin" supabase/migrations/0011_role_cleanup.sql | grep -c "has_role('ADMIN')"` is at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/boonsunquah/Supply chain project" && f=supabase/migrations/0011_role_cleanup.sql && test -f "$f" && [ "$(grep -c "ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'ACCOUNTS'" "$f")" = "1" ] && grep -q "ALTER COLUMN role DROP DEFAULT" "$f" && grep -q "UPDATE public.profiles" "$f" && ! grep -qE "^\s*(BEGIN|COMMIT);" "$f" && ! grep -nE "has_role\([^)]*('SUPER_ADMIN'|'GENERAL'|'LOGISTICS'|'WAREHOUSE')" "$f" && echo OK</automated>
  </verify>
  <done>0011 adds ACCOUNTS, remaps rows, drops the default, rewrites the trigger and is_super_admin(), and rebuilds every affected RLS policy on the four canonical roles with no deprecated-role grants.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 0012_drop_integration_tokens.sql (drop Shopee DB surface)</name>
  <read_first>
    - supabase/migrations/0010_integration_tokens.sql (defines integration_tokens and sync_log tables + their RLS policies)
    - .planning/phases/01-foundation-roles/01-RESEARCH.md Open Question #2 (recommendation: drop sync_log — it is Shopee-specific)
  </read_first>
  <action>
    Create a NEW append-only forward migration `supabase/migrations/0012_drop_integration_tokens.sql` per D-11. It drops the dead Shopee DB surface created in 0010: `DROP TABLE IF EXISTS integration_tokens CASCADE;` and `DROP TABLE IF EXISTS sync_log CASCADE;` (dropping the tables drops their RLS policies automatically). Per research Open Question #2, drop `sync_log` as well — it was only written by the now-deleted Shopee settings action (plan 01-01 removed the last writer). Add a top-of-file comment noting this is migration 0012 and removes the Shopee integration tables. Do NOT edit 0010 (append-only history).
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/0012_drop_integration_tokens.sql` exists
    - `grep -c "DROP TABLE IF EXISTS integration_tokens" supabase/migrations/0012_drop_integration_tokens.sql` is at least 1
    - `grep -c "DROP TABLE IF EXISTS sync_log" supabase/migrations/0012_drop_integration_tokens.sql` is at least 1
    - `supabase/migrations/0010_integration_tokens.sql` is unchanged (append-only — no edits to history)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/boonsunquah/Supply chain project" && f=supabase/migrations/0012_drop_integration_tokens.sql && test -f "$f" && grep -q "DROP TABLE IF EXISTS integration_tokens" "$f" && grep -q "DROP TABLE IF EXISTS sync_log" "$f" && echo OK</automated>
  </verify>
  <done>0012 drops integration_tokens and sync_log; migration 0010 is untouched.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply migrations to the live database (supabase db push)</name>
  <read_first>
    - supabase/migrations/0011_role_cleanup.sql (must exist from Task 1)
    - supabase/migrations/0012_drop_integration_tokens.sql (must exist from Task 2)
    - .planning/phases/01-foundation-roles/01-RESEARCH.md Environment Availability (Supabase CLI assumed; SQL Editor fallback)
  </read_first>
  <action>
    Apply the new migrations to the linked Supabase project so the live database reflects 0011 and 0012. Run `supabase db push`. For non-interactive execution, ensure `SUPABASE_ACCESS_TOKEN` is set in the environment and the project is linked (`supabase link`), or set `SUPABASE_DB_URL`. This step is MANDATORY: `npm run build` and type checks pass without it (types come from config, not the live DB), so skipping it creates a false-positive verification state where AUTH-01 is not actually satisfied in the database.

    If the CLI cannot push in this environment (no linked project, missing token, or interactive-only prompts), STOP and surface the blocker: paste the contents of `0011_role_cleanup.sql` then `0012_drop_integration_tokens.sql` (in that order) into the Supabase Dashboard SQL Editor and run them, OR have a human run `supabase db push` locally with the token set. Because this task is flagged `autonomous: false`, request human intervention rather than marking it done on a failed/again-skipped push.

    After applying, verify against the live DB that: the enum contains ACCOUNTS, no profiles row has a deprecated role, and integration_tokens/sync_log no longer exist (queries below).
  </action>
  <acceptance_criteria>
    - `supabase db push` reports the migrations applied (or the SQL Editor fallback ran both files without error)
    - Live DB check: `SELECT 1 FROM pg_enum e JOIN pg_type t ON t.oid=e.enumtypid WHERE t.typname='user_role' AND e.enumlabel='ACCOUNTS'` returns a row
    - Live DB check: `SELECT count(*) FROM public.profiles WHERE role IN ('SUPER_ADMIN','GENERAL','LOGISTICS','WAREHOUSE','SUPPLIER')` returns 0
    - Live DB check: `SELECT to_regclass('public.integration_tokens')` and `SELECT to_regclass('public.sync_log')` both return NULL
  </acceptance_criteria>
  <verify>
    <human-check>Confirm `supabase db push` (or the SQL Editor fallback) applied 0011 and 0012 to the linked project, and that the three live-DB checks above pass (ACCOUNTS present, zero deprecated-role rows, both Shopee tables gone). This is a release-gating manual step — paste the query results or the push output.</human-check>
  </verify>
  <done>Migrations 0011 and 0012 are applied to the live database; the enum, profile rows, and dropped tables verify as expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live Postgres | DDL/DML applied to production data via `supabase db push` |
| deprecated-role rows → ADMIN | Privilege remap; over-grant risk if a non-staff row is silently elevated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | Remapping GENERAL/SUPPLIER/etc. rows to ADMIN | mitigate | Brief specifies a small fixed staff user set (research Runtime State Inventory); the blocking human-check in Task 3 surfaces the post-migration profiles list so a human confirms no unexpected ADMIN elevation before sign-off |
| T-01-04 | Elevation of Privilege | RLS policies still referencing is_super_admin() after remap | mitigate | 0011 rewrites is_super_admin() to has_role('ADMIN') AND rebuilds every affected policy; acceptance criteria negative-grep deprecated-role grants |
| T-01-05 | Denial of Service | ALTER TYPE ADD VALUE inside a transaction block fails the whole migration | mitigate | Task 1 forbids BEGIN/COMMIT around ALTER TYPE; acceptance criteria assert no transaction wrapper |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Migrations 0011 and 0012 exist, are append-only (0001/0010 unchanged), and contain the required statements.
- `supabase db push` applied both migrations to the live project (blocking human-check).
- Live DB: ACCOUNTS in enum; zero deprecated-role profile rows; integration_tokens and sync_log dropped.
</verification>

<success_criteria>
- AUTH-01 (data layer): the user_role enum supports exactly the four canonical roles in use (ACCOUNTS added; deprecated roles carry no rows and no policy grants), profiles.role has no default, and the trigger requires an explicit role at invite time.
- The Shopee integration DB surface (integration_tokens, sync_log) is gone.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-roles/01-02-SUMMARY.md` when done.
</output>
