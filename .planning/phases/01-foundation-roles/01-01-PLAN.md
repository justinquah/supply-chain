---
phase: 01-foundation-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/sidebar.tsx
  - src/app/(authed)/projection/page.tsx
  - src/app/api/shopee/auth/route.ts
  - src/app/api/shopee/callback/route.ts
  - src/lib/shopee.ts
  - src/app/(authed)/settings/sync-button.tsx
  - src/app/(authed)/settings/page.tsx
  - src/app/(authed)/settings/actions.ts
  - src/components/nav-bar.tsx
  - src/app/(authed)/stock/actions.ts
  - src/app/(authed)/stock/page.tsx
  - src/app/(authed)/dashboard/page.tsx
  - src/_legacy/
  - shopee-submission/
  - prisma/
  - package.json
  - package-lock.json
autonomous: true
requirements: [FND-01, FND-02]

must_haves:
  truths:
    - "No file under src/ (outside deleted dirs) imports next-auth, @prisma/client, @libsql/client, bcryptjs, or @/lib/shopee"
    - "The /projection route and /api/shopee routes no longer exist in the app"
    - "The Settings page renders without importing any Shopee module"
    - "npm run build exits 0 with no module-resolution errors"
  artifacts:
    - path: "src/app/(authed)/settings/page.tsx"
      provides: "Shopee-free Settings page (placeholder for Admin user management added in 01-03)"
      contains: "Settings"
    - path: "src/app/(authed)/settings/actions.ts"
      provides: "Shopee-free settings server actions module"
    - path: "package.json"
      provides: "Dependency manifest with the 6 orphaned deps removed"
  key_links:
    - from: "src/components/nav-bar.tsx"
      to: "navigation array"
      via: "NAV array no longer contains the /projection entry"
      pattern: "projection"
---

<objective>
Remove all out-of-scope code (Shopee/marketplace sync, legacy forecasting/optimizer/scheduler tree, demand projection) and the 6 now-orphaned npm dependencies, leaving a clean, building codebase. This is the deck-clearing wave that unblocks the DB migration (01-02) and the role-model work (01-03).

Purpose: Satisfies FND-01 (Shopee/marketplace sync removed) and FND-02 (legacy demand-forecasting / container-optimizer / payment-scheduler deleted). It also eliminates the sole non-legacy `next-auth` importer (`sidebar.tsx`) so the dependency removal does not break the build (research Pitfall 2).
Output: Deleted directories, Shopee-free Settings stub, dependency-trimmed `package.json`/lockfile, green `npm run build`.
</objective>

<execution_context>
@/Users/boonsunquah/Supply chain project/.claude/gsd-core/workflows/execute-plan.md
@/Users/boonsunquah/Supply chain project/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-roles/01-CONTEXT.md
@.planning/phases/01-foundation-roles/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete out-of-scope directories and files; port sidebar off next-auth by deletion</name>
  <read_first>
    - src/components/layout/sidebar.tsx (the only non-legacy next-auth importer — confirm it is NOT rendered by the active layout)
    - src/app/(authed)/layout.tsx (confirms NavBar is rendered, not Sidebar)
    - src/components/nav-bar.tsx (the active nav; already form-POSTs to /auth/signout — supersedes sidebar)
    - src/app/auth/signout/route.ts (existing Supabase signout the app already uses)
  </read_first>
  <action>
    Delete outright (git history preserves them), per D-09:
    - Directory `src/_legacy/` (entire tree).
    - Directory `src/app/api/shopee/` (both `auth/route.ts` and `callback/route.ts`).
    - File `src/app/(authed)/projection/page.tsx` and its parent `src/app/(authed)/projection/` directory.
    - File `src/lib/shopee.ts`.
    - File `src/app/(authed)/settings/sync-button.tsx`.
    - File `src/components/layout/sidebar.tsx` (superseded by `NavBar`; sole non-legacy `next-auth` importer — delete BEFORE any dependency removal so the build never references `next-auth`).
    - Top-level directory `shopee-submission/` (verified empty — remove the directory entry).
    - Top-level directory `prisma/` (schema, migrations, seed, and `dev.db*` artifacts).
    Do NOT touch `supabase/migrations/` here — the integration_tokens / sync_log DB drop is migration work owned by plan 01-02.
  </action>
  <acceptance_criteria>
    - `test ! -e "src/_legacy"` (directory removed)
    - `test ! -e "src/app/api/shopee"` (directory removed)
    - `test ! -e "src/app/(authed)/projection"` (directory removed)
    - `test ! -e "src/lib/shopee.ts"`
    - `test ! -e "src/app/(authed)/settings/sync-button.tsx"`
    - `test ! -e "src/components/layout/sidebar.tsx"`
    - `test ! -e "prisma"` and `test ! -e "shopee-submission"`
    - `grep -rl "next-auth" src --include="*.ts" --include="*.tsx"` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/boonsunquah/Supply chain project" && for p in src/_legacy "src/app/api/shopee" "src/app/(authed)/projection" src/lib/shopee.ts "src/app/(authed)/settings/sync-button.tsx" src/components/layout/sidebar.tsx prisma shopee-submission; do test ! -e "$p" || { echo "STILL EXISTS: $p"; exit 1; }; done && test -z "$(grep -rl 'next-auth' src --include='*.ts' --include='*.tsx')" && echo OK</automated>
  </verify>
  <done>All listed paths are gone and no src file imports next-auth.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Settings to a Shopee-free stub and purge remaining Shopee/projection references</name>
  <read_first>
    - src/app/(authed)/settings/page.tsx (imports getShopeeTokens from @/lib/shopee, renders SyncButton, queries sync_log — all being removed)
    - src/app/(authed)/settings/actions.ts (exports syncShopeeStock, imports fetchShopeeStock from @/lib/shopee, writes sync_log — being removed)
    - src/components/nav-bar.tsx (NAV array contains a /projection entry to remove)
    - src/app/(authed)/stock/actions.ts (line ~31 has revalidatePath("/projection") to remove)
    - src/app/(authed)/stock/page.tsx (copy text mentions Shopee API / projection)
    - src/app/(authed)/dashboard/page.tsx (copy text "daily Shopee sync later")
  </read_first>
  <action>
    Rewrite `src/app/(authed)/settings/page.tsx` as a minimal Shopee-free Server Component: remove the `@/lib/shopee` import, the `SyncButton` import, the `sync_log` query, and all Shopee UI cards. Render a page titled "Settings" with a short placeholder note that user/role management lands here (plan 01-03 adds the Admin user-management UI to this exact file). Keep `getCurrentUser` if still used; do not add role gating yet (that is 01-03).
    Rewrite `src/app/(authed)/settings/actions.ts` to remove the `syncShopeeStock` export and the `@/lib/shopee` import entirely. Leave the file as a `"use server"` module with no Shopee logic (an empty `"use server"` module is acceptable; 01-03 adds inviteUser/updateUserRole here).
    In `src/components/nav-bar.tsx`, remove the `{ href: "/projection", label: "Projection" }` entry from the `NAV` array. Leave all other nav entries unchanged.
    In `src/app/(authed)/stock/actions.ts`, remove the `revalidatePath("/projection")` call (the route no longer exists).
    In `src/app/(authed)/stock/page.tsx` and `src/app/(authed)/dashboard/page.tsx`, update the user-facing copy strings that reference Shopee sync / projection so they no longer promise removed features (reword to describe weekly manual stock upload only). These are text-only edits — do not change query logic.
    Do NOT remove the `SHOPEE_API` value from the `stock_source` enum or any DB object — enum/DB changes are plan 01-02's scope; here we only stop referencing Shopee in app code and copy.
  </action>
  <acceptance_criteria>
    - `grep -rl "@/lib/shopee\|getShopeeTokens\|fetchShopeeStock\|syncShopeeStock\|sync_log\|SyncButton" src --include="*.ts" --include="*.tsx"` returns no matches
    - `grep -n "/projection" src/components/nav-bar.tsx` returns no matches
    - `grep -n "revalidatePath(\"/projection\")" "src/app/(authed)/stock/actions.ts"` returns no matches
    - `src/app/(authed)/settings/page.tsx` still exports a default React component (file present, no `@/lib/shopee` import)
    - `src/app/(authed)/settings/actions.ts` begins with the `"use server"` directive
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/boonsunquah/Supply chain project" && test -z "$(grep -rl '@/lib/shopee\|getShopeeTokens\|fetchShopeeStock\|syncShopeeStock\|SyncButton' src --include='*.ts' --include='*.tsx')" && ! grep -q "/projection" src/components/nav-bar.tsx && head -1 "src/app/(authed)/settings/actions.ts" | grep -q "use server" && echo OK</automated>
  </verify>
  <done>Settings is Shopee-free, the projection nav entry and revalidate are gone, and no src file references any Shopee symbol.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the 6 orphaned dependencies and confirm a green build</name>
  <read_first>
    - package.json (confirm next-auth, @prisma/client, @prisma/adapter-libsql, prisma, @libsql/client, bcryptjs are present; note @types/bcryptjs in devDependencies)
    - .planning/phases/01-foundation-roles/01-RESEARCH.md (Packages Being Removed table; removal command)
  </read_first>
  <action>
    Per D-10, remove the 6 orphaned packages now that no live importer remains (Tasks 1-2 removed the last ones). Run `npm uninstall next-auth @prisma/client @prisma/adapter-libsql prisma @libsql/client bcryptjs`. Also remove the now-dead `@types/bcryptjs` from `devDependencies` in the same uninstall (`npm uninstall @types/bcryptjs`). Let npm update `package.json` and `package-lock.json`. Do NOT add any new dependency — this wave is removal-only, honoring the project tech-stack lock. After uninstall, run `npm run build` and confirm it exits 0. If the build surfaces a residual import of any removed package, fix that importer (it indicates a missed reference from Tasks 1-2) and rebuild.
  </action>
  <acceptance_criteria>
    - `node -e "const d={...require('./package.json').dependencies,...require('./package.json').devDependencies}; for (const k of ['next-auth','@prisma/client','@prisma/adapter-libsql','prisma','@libsql/client','bcryptjs','@types/bcryptjs']) if (d[k]) { console.error('still present: '+k); process.exit(1);} "` exits 0
    - `npm run build` exits 0
    - `@supabase/supabase-js`, `@supabase/ssr`, `next`, `react`, `zod`, `xlsx` remain present in dependencies (no accidental removal of the locked stack)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/boonsunquah/Supply chain project" && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; for (const k of ['next-auth','@prisma/client','@prisma/adapter-libsql','prisma','@libsql/client','bcryptjs','@types/bcryptjs']) if (d[k]) { console.error('still present: '+k); process.exit(1);} for (const k of ['@supabase/supabase-js','@supabase/ssr','next','react','zod','xlsx']) if (!d[k]) { console.error('missing locked dep: '+k); process.exit(1);} console.log('deps OK');" && npm run build</automated>
  </verify>
  <done>The 6 orphaned deps (and @types/bcryptjs) are gone from package.json/lockfile, the locked stack remains, and npm run build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | Code/file deletions; integrity tracked by git history (recoverable) |
| npm registry → build | Dependency removal could break the build if a live importer remains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Removing next-auth/prisma/libsql/bcryptjs before all importers are gone | mitigate | Task 1 deletes sidebar.tsx (sole non-legacy next-auth importer) and all Shopee/legacy code BEFORE Task 3 uninstalls; Task 3 gates on `grep` returning zero importers and `npm run build` exit 0 |
| T-01-02 | Tampering | Accidental removal of a locked-stack dependency | accept | Task 3 acceptance criteria positively assert @supabase/*, next, react, zod, xlsx remain present |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This wave performs removal only — no new packages are installed; RESEARCH.md Package Legitimacy Audit confirms no additions |
</threat_model>

<verification>
- `grep -rl "next-auth\|@/lib/shopee\|@prisma\|@libsql\|bcryptjs" src --include="*.ts" --include="*.tsx"` returns no matches.
- The deleted directories/files (src/_legacy, src/app/api/shopee, src/app/(authed)/projection, src/lib/shopee.ts, sidebar.tsx, prisma, shopee-submission, sync-button.tsx) no longer exist.
- `npm run build` exits 0.
</verification>

<success_criteria>
- FND-01: All Shopee/marketplace sync code (api/shopee routes, lib/shopee.ts, sync-button.tsx, syncShopeeStock action) is removed from the app.
- FND-02: Legacy demand-forecasting / optimizer / scheduler tree (src/_legacy) and the demand projection page are deleted.
- The 6 orphaned dependencies are removed; the build is green on the locked stack only.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-roles/01-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

This phase-level registry lists every NEW symbol/path the Phase 1 plans (01-01, 01-02, 01-03) create. The source-grounding / drift pass uses this so newly-created symbols are not flagged as undefined references.

### New files (created in this phase)
- `supabase/migrations/0011_role_cleanup.sql` — plan 01-02 (ACCOUNTS enum add, deprecated-role row remap, default drop, handle_new_user trigger update, is_super_admin() rewrite, RLS policy rebuild)
- `supabase/migrations/0012_drop_integration_tokens.sql` — plan 01-02 (drops integration_tokens and sync_log)
- `src/lib/supabase/admin.ts` — plan 01-03 (exports `createAdminClient()`, service-role, server-only)
- `src/app/(authed)/settings/users-table.tsx` — plan 01-03 (client component: user list + invite + per-row role change)

### New exported symbols / identifiers
- `requireRole(...roles: AppRole[])` — plan 01-03, added to `src/lib/supabase/server.ts`
- `AppRole` type (`"SCM" | "ACCOUNTS" | "FINANCE" | "ADMIN"`) — plan 01-03, `src/lib/supabase/server.ts`
- `createAdminClient()` — plan 01-03, `src/lib/supabase/admin.ts`
- `appRoleSchema` (zod enum of the four roles) — plan 01-03, `src/types/index.ts`
- `inviteUser(email, role, name)` server action — plan 01-03, `src/app/(authed)/settings/actions.ts`
- `updateUserRole(userId, role)` server action — plan 01-03, `src/app/(authed)/settings/actions.ts`

### New database objects / values
- `ACCOUNTS` value on the `user_role` enum — plan 01-02

### New environment variables
- `SUPABASE_SERVICE_ROLE_KEY` — server-only; consumed by `createAdminClient()` (plan 01-03); set in Vercel production (FND-03 / AUTH-03)
- `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` — pre-existing; this phase adds defensive guards and documents them as required in Vercel (FND-03)

### Removed (no longer present after this phase)
- Dependencies: `next-auth`, `@prisma/client`, `@prisma/adapter-libsql`, `prisma`, `@libsql/client`, `bcryptjs`, `@types/bcryptjs` (plan 01-01)
- Paths: `src/_legacy/`, `src/app/api/shopee/`, `src/app/(authed)/projection/`, `src/lib/shopee.ts`, `src/app/(authed)/settings/sync-button.tsx`, `src/components/layout/sidebar.tsx`, `prisma/`, `shopee-submission/` (plan 01-01)
- DB objects: `integration_tokens`, `sync_log` tables (plan 01-02); `syncShopeeStock` action, `getShopeeTokens`/`fetchShopeeStock` (plan 01-01)
